# Specialized Content Safety Models (Optional - for enhanced thumbnail scanning)
# Install with: pip install -r requirements_specialized.txt

# Core dependencies (should already be installed)
opencv-python>=4.9.0.80
numpy>=1.26.4
ultralytics>=8.0.0
Pillow>=10.0.0

# Specialized Content Safety Models (Hugging Face Transformers)
# These models provide better detection of horror, gore, violence, and inappropriate content
transformers>=4.30.0
torch>=2.0.0
torchvision>=0.15.0

# Optional: TensorFlow Hub models (alternative approach)
# tensorflow>=2.13.0
# tensorflow-hub>=0.14.0

# Note: The specialized models will auto-download on first use
# Models tried in order:
# 1. Falconsai/nsfw_image_detection - Primary model for NSFW/violence detection
# 2. openai/clip-vit-base-patch32 - Fallback CLIP model for semantic understanding
# 
# The system will automatically try each model and use the first one that loads successfully.

